name: Deploy TorchServe Image to GHCR

on:
  push:
    branches:
      - issue/11-torchserve-ghcr-workflow

jobs:
  build_and_test:
    runs-on: ubuntu-latest

    steps:
      - name: 'Checkout GitHub Action'
        uses: actions/checkout@main

      - name: 'Build Inventory Image'
        run: |
            docker build -t test-image -f ./torchserve/Dockerfile.test ./torchserve

      - name: 'Run tests'
        env:
          IMAGE_NAME: test-image
        run: | 
          pip install -r ./torchserve/tests/requirements.txt
          python3 -m pytest ./torchserve

  push: 
    if: github.repository == 'scilifelabdatacentre/serve-images'
    needs: build_and_test
    runs-on: ubuntu-latest
    concurrency:
      group: '${{ github.workflow }} @ ${{ github.event.pull_request.head.label || github.head_ref || github.ref }}'
      cancel-in-progress: true
    permissions:
      contents: read
      packages: write
      
    steps:
      - name: 'Checkout GitHub Action'
        uses: actions/checkout@main

      - name: 'Login to GitHub Container Registry'
        uses: docker/login-action@v1
        with:
          registry: ghcr.io
          username: ${{github.actor}}
          password: ${{secrets.GITHUB_TOKEN}}

      - name: Push to GHCR
        run: |
          docker build -t ghcr.io/scilifelabdatacentre/rstudio:latest ./torchserve